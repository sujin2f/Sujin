{
    "name": "sujin",
    "version": "12.0.0",
    "description": "React + Node + WordPress theme for sujinc.com",
    "license": "MIT",
    "scripts": {
        "install-modules": "yarn --cwd ./@auth && yarn --cwd ./@common && yarn --cwd ./@graphql && yarn --cwd ./@lib && yarn --cwd ./@next && yarn --cwd ./@wordpress/plugins/resume-item",
        "build": "yarn --cwd ./@auth build && yarn --cwd ./@common build && yarn --cwd ./@lib build && yarn --cwd ./@wordpress/plugins/resume-item build",
        "lint": "yarn lint:auth && yarn lint:common && yarn lint:gql && yarn lint:lib && yarn lint:next",
        "lint:auth": "yarn --cwd ./@auth lint",
        "lint:common": "yarn --cwd ./@common lint",
        "lint:gql": "yarn --cwd ./@graphql lint",
        "lint:lib": "yarn --cwd ./@lib lint",
        "lint:next": "yarn --cwd ./@next lint",
        "package": "yarn package:wordpress",
        "package:wordpress": "yarn --cwd ./@wordpress package",
        "test": "yarn --cwd ./@common test && yarn --cwd ./@graphql test",
        "prepare": "husky"
    },
    "devDependencies": {
        "husky": "^9.1.7"
    }
}
