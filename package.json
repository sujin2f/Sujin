{
    "name": "sujin",
    "version": "11.0.0",
    "description": "React + Node + WordPress theme for sujinc.com",
    "license": "MIT",
    "scripts": {
        "install-modules": "yarn --cwd ./@common && yarn --cwd ./@graphql && yarn --cwd ./@lib && yarn --cwd ./@next && yarn --cwd ./@wordpress",
        "build": "yarn --cwd ./@common build && yarn --cwd ./@lib build && yarn --cwd ./@wordpress/plugins/resume-item build",
        "lint": "yarn lint:common && yarn lint:gql && yarn lint:lib && yarn lint:next",
        "lint:common": "yarn --cwd ./@common lint",
        "lint:gql": "yarn --cwd ./@graphql lint",
        "lint:lib": "yarn --cwd ./@lib lint",
        "lint:next": "yarn --cwd ./@next lint",
        "test": "yarn --cwd ./@common test && yarn --cwd ./@graphql test",
        "prepare": "husky"
    },
    "devDependencies": {
        "husky": "^9.1.7"
    }
}
