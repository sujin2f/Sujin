{
    "name": "sujin",
    "version": "12.1.0",
    "description": "React + Node + WordPress theme for sujinc.com",
    "license": "MIT",
    "scripts": {
        "install-modules": "yarn --cwd ./@common && yarn --cwd ./@lib && yarn --cwd ./@next && yarn --cwd ./@wordpress install-modules",
        "build": "yarn --cwd ./@common build && yarn --cwd ./@lib build && yarn --cwd ./@wordpress build",
        "lint": "yarn lint:common && yarn lint:lib && yarn lint:next",
        "lint:common": "yarn --cwd ./@common lint",
        "lint:lib": "yarn --cwd ./@lib lint",
        "lint:next": "yarn --cwd ./@next lint",
        "test": "yarn --cwd ./@common test && yarn --cwd ./@next test",
        "prepare": "husky"
    },
    "devDependencies": {
        "husky": "^9.1.7"
    }
}
