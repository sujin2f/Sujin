import { Image } from '@src/schema/image'

export const ImageSize = {
    medium: Image,
    mediumLarge: Image,
    large: Image,
    thumbnail: Image,
    postThumbnail: Image,
    relatedPost: Image,
    recentPost: Image,
}
