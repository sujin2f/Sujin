export enum Scalar {
    INT = 'Int',
    FLOAT = 'Float',
    STRING = 'String',
    BOOLEAN = 'Boolean',
    ID = 'ID',
}

export enum OperationType {
    QUERY = 'query',
    MUTATION = 'mutation',
}
