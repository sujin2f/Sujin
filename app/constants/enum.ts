export enum Scrolled {
  Yes = 'scrolled',
  No = '',
}

export enum MobileMenu {
  Yes = 'mobile-menu',
  No = '',
}

export enum TermTypes {
  Category = 'category',
  Tag = 'tag',
  Search = 'search',
}
