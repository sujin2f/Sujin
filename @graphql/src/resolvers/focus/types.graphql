type Query {
    focusBookmarks: [FocusMessage]
}
type Mutation {
    createFocusBookmark(message: FocusMessageInput!): String
    removeFocusBookmark(id: String!): String
}

type FocusMessage {
    _id: String
    key: String
    title: String
    device: String
    messageType: String
    message: String
}

input FocusMessageInput {
    key: String
    title: String
    device: String
    messageType: String
    message: String
}
