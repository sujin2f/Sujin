query {
    recipe(_id: "?") {
        title
    }
    recipes(page: 1) {
        title
    }
}

mutation {
    id: createRecipe(recipe: {})
    id: updateRecipe(recipe: {})
    removeRecipe(_id: "?")
}
