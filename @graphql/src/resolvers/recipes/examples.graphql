query {
    recipe(_id: "?") {
        title
    }
    recipes(page: 1) {
        title
    }
}

mutation {
    id: createRecipe(recipe: {})
    id: replaceRecipe(recipe: {})
    removeRecipe(_id: "?")
}
