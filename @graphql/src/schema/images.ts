import { ImageBlock } from '@src/schema/image-block'

export const Images = {
    list: ImageBlock,
    icon: ImageBlock,
    title: ImageBlock,
    background: ImageBlock,
    thumbnail: ImageBlock,
}
