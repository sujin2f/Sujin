/**
 * WP Post types
 * @enum
 */
export enum POST_TYPE {
    POST = 'post',
    PAGE = 'page',
    ATTACHMENT = 'attachment',
}

/**
 * @enum
 * @todo use this
 */
export enum POST_STATUS {
    PUBLISH = 'publish',
    DRAFT = 'draft',
    TRASH = 'trash',
}
