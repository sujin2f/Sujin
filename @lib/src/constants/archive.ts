/**
 * Archive types
 * @enum
 */
export enum ARCHIVE {
    CATEGORY = 'category',
    TAG = 'tag',
    SEARCH = 'search',
}

/**
 * WP Taxonomies
 * @enum
 */
export enum TAXONOMY {
    CATEGORY = 'category',
    TAG = 'tag',
    POST_TAG = 'post_tag',
}
