query ListSpectrum($number: Int!, $ion: Int!) {
    spectrum(number: $number, ion: $ion) {
        number
        ion
        energy
        spin
        l
        parity
        j
        base
        conf
        eConf
        ionReverse
        position
        term
        orbital
    }
}
